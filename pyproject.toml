[project]
name = "rivian-fleet-api"
version = "0.1.0"
description = "Fleet and vehicle management platform for Rivian's connected vehicle ecosystem"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
